syntax = "proto3";

option go_package = "github.com/jyellick/examples/protos/common";

package common;

enum Status {
    UNKNOWN = 0;
    SUCCESS = 200;
    NOT_FOUND = 404;
}

message Reply {
    Status status = 1;
    string message = 2; 
}
